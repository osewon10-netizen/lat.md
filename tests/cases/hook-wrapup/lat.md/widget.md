# Widget

Describes the widget module and the contract its renderer must uphold.

## Rendering

The renderer in [[src/widget.ts#renderWidget]] returns the widget markup as a
string and never mutates its input.
